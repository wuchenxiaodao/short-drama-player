# 短剧互动播放器 — 二轮问题分析与修复指南

> 基于 [wuchenxiaodao/short-drama-player](https://github.com/wuchenxiaodao/short-drama-player) 代码审查
> 
> 审查范围：逐文件验证已完成的改进，发现遗漏问题，补充新发现的问题

---

## 一、已改进项验证状态

| # | 改进项 | 状态 | 备注 |
|---|--------|------|------|
| 1 | JWT鉴权体系 | ✅ 已完成 | JwtUtil/JwtFilter/SecurityConfig/AuthController/AuthService 均已实现 |
| 2 | BCrypt密码加密 | ✅ 已完成 | PasswordEncoder Bean + DataInitializer 使用 encode |
| 3 | InteractionOption拆表 | ✅ 已完成 | optionsJson 已移除，InteractionOption 独立实体 |
| 4 | BusinessException + GlobalExceptionHandler | ✅ 已完成 | 所有 Service 已使用 BusinessException（但 GlobalExceptionHandler 源码无法访问确认完整性） |
| 5 | AuthUtils工具类 | ✅ 已完成 | getCurrentUserId() + requireUserId() |
| 6 | 多集互动 | ✅ 已完成 | 每部剧前2-5集均有互动点 |
| 7 | 前置条件过滤 | ✅ 已完成 | prerequisite + prerequisiteChoiceOptionId |
| 8 | 前端JWT集成 | ✅ 已完成 | preview.html 已集成 token 管理 |
| 9 | docker-compose密码环境变量 | ✅ 已完成 | MYSQL_ROOT_PASSWORD/REDIS_PASSWORD 均使用 ${VAR:-default} |
| 10 | RatingRepository.countByDramaIds() | ✅ 已完成 | 批量查询方法存在 |
| 11 | WatchProgressRepository.findByUserIdAndEpisodeIdIn() | ✅ 已完成 | 批量查询方法存在 |
| 12 | DramaRepository.incrementViewCount() | ✅ 已完成 | 原子操作方法存在 |
| 13 | 新功能(Comment/Rating/Online/UserEgg) | ✅ 已完成 | Controller/Service/Model 均已实现 |

### 关键修正：部分原始"仍然存在的问题"实际已修复

- **原始问题 #1（getWatchProgress N+1）**：✅ **已修复**。当前代码已使用 `findByUserIdAndEpisodeIdIn()` 批量查询
- **原始问题 #9（AnswerRequest.userId多余）**：✅ **已修复**。AnswerRequest 中 userId 字段已被移除，只剩 interactionId 和 choiceId

---

## 二、仍然存在的问题（按严重程度排序）

### 🔴 严重（功能缺陷 / 安全隐患）

---

#### S1. DramaService.syncViewCountToDatabase() — 读-改-写并发不安全

**位置：** `DramaService.java` syncViewCountToDatabase() 方法

**问题：** Redis 计数每满100同步到数据库时，仍使用读-改-写模式：
```java
private void syncViewCountToDatabase(Long dramaId, Long increment) {
    dramaRepository.findById(dramaId).ifPresent(d -> {
        d.setViewCount(d.getViewCount() + increment);  // 并发不安全！
        dramaRepository.save(d);
    });
}
```
如果两个 Redis key 同时满100触发同步，可能出现计数丢失。虽然非 Redis 路径已使用 `incrementViewCount()` 原子操作，但 Redis→DB 的同步路径仍不安全。

**修复代码：**

```java
// DramaRepository.java — 添加批量增量方法
@Modifying
@Transactional
@Query("UPDATE Drama d SET d.viewCount = d.viewCount + :increment WHERE d.id = :id")
void incrementViewCountBy(@Param("id") Long id, @Param("increment") Long increment);

// DramaService.java — 修改同步方法
private void syncViewCountToDatabase(Long dramaId, Long increment) {
    dramaRepository.incrementViewCountBy(dramaId, increment);
}
```

---

#### S2. EpisodeController.playInfo() — userId 仍从 query param 获取

**位置：** `EpisodeController.java`

**问题：** `@RequestParam(required = false) Long userId` 没有用 AuthUtils，任何人可传任意 userId 获取他人观看进度。DramaController.detail() 已正确使用 `AuthUtils.getCurrentUserId()`，但 EpisodeController 未跟进。

**修复代码：**

```java
@GetMapping("/{id}/playinfo")
public ApiResponse<PlayInfo> playInfo(@PathVariable Long id) {
    return ApiResponse.success(episodeService.getPlayInfo(id, AuthUtils.getCurrentUserId()));
}
```

---

#### S3. OnlineController.heartbeat() — userId 从请求体获取，任何人可伪造

**位置：** `OnlineController.java`

**问题：** `Long userId = Long.valueOf(body.get("userId").toString())` 没有认证。同时 SecurityConfig 中 `/api/online/**` 是 permitAll，任何人可以伪造任意 userId 上报在线状态。

**修复代码：**

```java
@PostMapping("/heartbeat")
public Map<String, Object> heartbeat(@RequestBody Map<String, Object> body) {
    Long userId = AuthUtils.requireUserId();  // 从 JWT 获取，而非请求体
    Long episodeId = Long.valueOf(body.get("episodeId").toString());
    onlineService.heartbeat(userId, episodeId);
    return Map.of("success", true);
}
```

同时修改 SecurityConfig，将 `/api/online/**` 从 permitAll 移除，仅保留 GET 接口公开：

```java
// SecurityConfig.java
.requestMatchers("/api/online/episode/**").permitAll()  // 仅 GET 查询公开
// POST /api/online/heartbeat 走 anyRequest().authenticated()
```

---

#### S4. InteractionPoint.options LAZY + 无事务 — 可能 LazyInitializationException

**位置：** `EpisodeService.java` buildInteractionInfo() 方法

**问题：** `InteractionPoint.options` 是 `FetchType.LAZY`，`interactionPointRepository.findByEpisodeIdOrderByTimestampMsAsc()` 查询后，在无 `@Transactional` 的方法中访问 `point.getOptions()`，如果 Session 已关闭会抛 LazyInitializationException。当前可能因为 Spring Boot 的 OpenEntityManagerInView 而侥幸工作，但这是反模式且在部署到非默认配置时会崩溃。

**修复代码（方案一：JOIN FETCH 查询，推荐）：**

```java
// InteractionPointRepository.java — 添加 JOIN FETCH 查询
@Query("SELECT ip FROM InteractionPoint ip LEFT JOIN FETCH ip.options WHERE ip.episode.id = :episodeId ORDER BY ip.timestampMs ASC")
List<InteractionPoint> findWithOptionsByEpisodeId(@Param("episodeId") Long episodeId);

// EpisodeService.java — 使用新查询方法
private List<PlayInfo.InteractionInfo> buildInteractionInfoList(Long episodeId, Long userId) {
    List<InteractionPoint> points = interactionPointRepository.findWithOptionsByEpisodeId(episodeId);
    // ... 后续逻辑不变
}
```

---

#### S5. Redis viewCount 1小时过期 — 数据丢失风险

**位置：** `DramaService.java` incrementViewCountWithRedis()

**问题：** `redisTemplate.expire(key, 1, TimeUnit.HOURS)` — 如果1小时内增量未满100次，中间数据在 key 过期后丢失。低流量短剧的播放量可能永远无法同步到数据库。

**修复代码：**

```java
private void incrementViewCountWithRedis(Long dramaId) {
    String key = "drama:view:" + dramaId;
    Long count = redisTemplate.opsForValue().increment(key);
    // 不再设置1小时过期，改为持久存储
    // 每10次就同步一次到数据库，降低数据丢失风险
    if (count != null && count % 10 == 0) {
        syncViewCountToDatabase(dramaId, 10L);
        // 同步后重置 Redis 计数
        redisTemplate.opsForValue().set(key, "0");
    }
}
```

---

#### S6. AuthController.getUser() — 任意用户信息可被未认证访问

**位置：** `AuthController.java` `GET /api/auth/{userId}`

**问题：** SecurityConfig 中 `/api/auth/**` 全部 permitAll，导致 `GET /api/auth/{userId}` 可被任何人访问。攻击者可遍历 userId 获取所有用户的用户名、昵称、积分等信息。

**修复代码：**

```java
// 方案一：删除此接口（推荐，/api/auth/me 已能获取当前用户信息）
// 直接删除 AuthController 中的 getUser() 方法

// 方案二：如果需要管理员查看其他用户，添加路径控制
// SecurityConfig.java
.requestMatchers("/api/auth/register", "/api/auth/login", "/api/auth/me").permitAll()
// /api/auth/{userId} 走 anyRequest().authenticated()
```

---

#### S7. JwtUtil 默认密钥不安全

**位置：** `JwtUtil.java`

**问题：** `@Value("${jwt.secret:defaultSecretKeyMustBeAtLeast32BytesLong!!}")` — 如果部署时未配置 `jwt.secret`，将使用硬编码默认值。攻击者可伪造任意用户的 JWT token。

**修复代码：**

```java
@Component
public class JwtUtil {
    @Value("${jwt.secret}")  // 移除默认值，强制配置
    private String secret;

    @PostConstruct
    public void validateSecret() {
        if (secret == null || secret.length() < 32) {
            throw new IllegalStateException("jwt.secret 必须配置且至少32字符");
        }
        if ("defaultSecretKeyMustBeAtLeast32BytesLong!!".equals(secret)) {
            throw new IllegalStateException("jwt.secret 不能使用默认值，请在配置文件中设置安全密钥");
        }
    }
    // ... 其余不变
}
```

---

#### S8. JwtFilter 静默吞掉所有异常

**位置：** `JwtFilter.java`

**问题：** `catch (Exception ignored) {}` — 所有 JWT 验证异常（包括 ExpiredJwtException、SignatureException 等）被静默忽略。这使得：
1. 调试困难，无法定位 token 问题
2. 安全相关的异常（如签名篡改）也不会记录日志

**修复代码：**

```java
@Override
protected void doFilterInternal(HttpServletRequest request,
                                 HttpServletResponse response,
                                 FilterChain filterChain)
        throws ServletException, IOException {
    String header = request.getHeader("Authorization");
    if (header != null && header.startsWith("Bearer ")) {
        String token = header.substring(7);
        try {
            Long userId = jwtUtil.validateToken(token);
            UsernamePasswordAuthenticationToken auth =
                new UsernamePasswordAuthenticationToken(userId, null, List.of());
            SecurityContextHolder.getContext().setAuthentication(auth);
        } catch (io.jsonwebtoken.ExpiredJwtException e) {
            log.debug("JWT token expired: {}", e.getMessage());
            // 不设置认证，请求将被 Spring Security 拦截返回 401
        } catch (io.jsonwebtoken.SecurityException e) {
            log.warn("JWT security violation: {}", e.getMessage());
        } catch (Exception e) {
            log.warn("JWT validation failed: {}", e.getMessage());
        }
    }
    filterChain.doFilter(request, response);
}
```

需要在类上添加 `@Slf4j` 注解。

---

### 🟡 中等（代码质量 / 一致性 / 潜在 Bug）

---

#### M1. DramaService.getRelatedDramas() — 小规模 N+1 ratingCount

**位置：** `DramaService.java` getRelatedDramas() → toSummary(d)

**问题：** `mapToSummaryPage()` 已使用批量 `countByDramaIds()`，但 `getRelatedDramas()` 调用的 `toSummary(Drama d)` 单参数版本仍逐条查询 `ratingRepository.getRatingCount(d.getId())`。虽然最多6条，但属于不一致。

**修复代码：**

```java
private List<DramaSummary> getRelatedDramas(Drama drama) {
    List<Drama> relatedDramas = dramaRepository.findByCategoryAndIdNot(
            drama.getCategory(), drama.getId(), PageRequest.of(0, 6));
    if (relatedDramas.isEmpty()) return List.of();
    // 批量查询 ratingCount
    List<Long> ids = relatedDramas.stream().map(Drama::getId).collect(Collectors.toList());
    List<Object[]> counts = ratingRepository.countByDramaIds(ids);
    Map<Long, Long> ratingCountMap = new HashMap<>();
    for (Object[] row : counts) {
        ratingCountMap.put((Long) row[0], (Long) row[1]);
    }
    return relatedDramas.stream()
            .map(d -> toSummary(d, ratingCountMap.getOrDefault(d.getId(), 0L)))
            .collect(Collectors.toList());
}
```

---

#### M2. RatingController.submit() — 使用 Map<String,Object> 而非 DTO

**位置：** `RatingController.java`

**问题：** 使用 `@RequestBody Map<String, Object> body` 并手动类型转换，无参数校验。若 body 缺少 dramaId 或 score 字段会抛 NPE/ClassCastException。

**修复代码：**

```java
// 新建 RatingRequest.java
package com.drama.dto;

import jakarta.validation.constraints.Max;
import jakarta.validation.constraints.Min;
import jakarta.validation.constraints.NotNull;
import lombok.Data;

@Data
public class RatingRequest {
    @NotNull(message = "短剧ID不能为空")
    private Long dramaId;
    @NotNull(message = "评分不能为空")
    @Min(value = 1, message = "评分最低1分")
    @Max(value = 10, message = "评分最高10分")
    private Integer score;
}

// RatingController.java
@PostMapping("/submit")
public ApiResponse<Map<String, Object>> submit(@Valid @RequestBody RatingRequest request) {
    Long userId = AuthUtils.requireUserId();
    // ... 后续逻辑用 request.getDramaId() / request.getScore()
}
```

---

#### M3. CommentRequest — 缺少参数校验

**位置：** `CommentRequest.java`

**问题：** interactionId、content、parentCommentId 均无 `@NotNull`/`@NotBlank` 校验。content 可以为空字符串，interactionId 可以为 null 导致 NPE。

**修复代码：**

```java
@Data
public class CommentRequest {
    @NotNull(message = "互动ID不能为空")
    private Long interactionId;
    @NotBlank(message = "评论内容不能为空")
    @Size(max = 500, message = "评论内容不能超过500字")
    private String content;
    private Long parentCommentId;  // 可选，回复时才有
}
```

Controller 的 `postComment` 方法需添加 `@Valid`（已有则无需修改）。

---

#### M4. CommentService.toResponse() — N+1 查询

**位置：** `CommentService.java` toResponse()

**问题：** 每条评论都调用 `userRepository.findById(comment.getUserId())` 获取昵称，以及 `commentLikeRepository.findByUserIdAndCommentId()` 检查点赞状态。20条评论 = 40次额外查询。

**修复代码：**

```java
public CommentResponse.PageResult getComments(Long interactionId, String sort, int page, int size) {
    Page<Comment> commentPage = ...;
    List<Comment> comments = commentPage.getContent();
    
    // 批量加载用户昵称
    List<Long> userIds = comments.stream().map(Comment::getUserId).distinct().collect(Collectors.toList());
    Map<Long, User> userMap = userRepository.findAllById(userIds).stream()
            .collect(Collectors.toMap(User::getId, u -> u));
    
    // 批量加载点赞状态（如果有登录用户）
    Long currentUserId = AuthUtils.getCurrentUserId();
    Set<Long> likedCommentIds = Set.of();
    if (currentUserId != null) {
        List<Long> commentIds = comments.stream().map(Comment::getId).collect(Collectors.toList());
        likedCommentIds = commentLikeRepository.findByUserIdAndCommentIdIn(currentUserId, commentIds)
                .stream().map(CommentLike::getCommentId).collect(Collectors.toSet());
    }
    
    Set<Long> finalLiked = likedCommentIds;
    List<CommentResponse> responses = comments.stream()
            .map(c -> toResponse(c, userMap.get(c.getUserId()), finalLiked.contains(c.getId())))
            .collect(Collectors.toList());
    // ...
}

private CommentResponse toResponse(Comment comment, User author, boolean isLiked) {
    CommentResponse resp = new CommentResponse();
    resp.setId(comment.getId());
    resp.setUserId(comment.getUserId());
    resp.setContent(comment.getContent());
    resp.setLikeCount(comment.getLikeCount());
    resp.setCreatedAt(comment.getCreatedAt());
    resp.setNickname(author != null ? author.getNickname() : "未知用户");
    resp.setIsLiked(isLiked);
    return resp;
}
```

需要添加 `CommentLikeRepository.findByUserIdAndCommentIdIn()` 方法。

---

#### M5. CommentService.getComments() — replyCountMap 计算逻辑错误

**位置：** `CommentService.java` getComments()

**问题：** 
```java
Map<Long, Long> replyCountMap = comments.stream().collect(Collectors.toMap(
    Comment::getId,
    c -> commentRepository.countByInteractionId(interactionId) // 每条评论都映射到总评论数！
));
```
1. 每条评论的 replyCount 被设为该互动的总评论数，而非该评论的回复数
2. 计算结果 `replyCountMap` 从未被使用（responses 映射中没引用它）

**修复代码：**

```java
// 添加 Repository 方法
// CommentRepository.java
@Query("SELECT c.parentCommentId, COUNT(c) FROM Comment c WHERE c.parentCommentId IN :parentIds GROUP BY c.parentCommentId")
List<Object[]> countRepliesByParentIds(@Param("parentIds") List<Long> parentIds);

// CommentService.java
List<Long> commentIds = comments.stream().map(Comment::getId).collect(Collectors.toList());
Map<Long, Long> replyCountMap = new HashMap<>();
if (!commentIds.isEmpty()) {
    List<Object[]> replyCounts = commentRepository.countRepliesByParentIds(commentIds);
    for (Object[] row : replyCounts) {
        replyCountMap.put((Long) row[0], (Long) row[1]);
    }
}
// 在 toResponse 中传入 replyCount
```

---

#### M6. CommentService.toggleLike() — likeCount 读-改-写并发不安全

**位置：** `CommentService.java` toggleLike()

**问题：** `comment.setLikeCount(comment.getLikeCount() + 1)` 和 `comment.setLikeCount(Math.max(0, comment.getLikeCount() - 1))` 都是读-改-写，并发点赞/取消点赞可能导致计数不准确。

**修复代码：**

```java
// CommentRepository.java — 添加原子操作
@Modifying
@Transactional
@Query("UPDATE Comment c SET c.likeCount = c.likeCount + 1 WHERE c.id = :id")
void incrementLikeCount(@Param("id") Long id);

@Modifying
@Transactional
@Query("UPDATE Comment c SET c.likeCount = GREATEST(c.likeCount - 1, 0) WHERE c.id = :id")
void decrementLikeCount(@Param("id") Long id);

// CommentService.java
@Transactional
public boolean toggleLike(Long userId, Long commentId) {
    var existing = commentLikeRepository.findByUserIdAndCommentId(userId, commentId);
    if (existing.isPresent()) {
        commentLikeRepository.delete(existing.get());
        commentRepository.decrementLikeCount(commentId);
        return false;
    } else {
        CommentLike like = new CommentLike();
        like.setUserId(userId);
        like.setCommentId(commentId);
        commentLikeRepository.save(like);
        commentRepository.incrementLikeCount(commentId);
        return true;
    }
}
```

---

#### M7. RatingController.submit() — Drama.rating 更新也是读-改-写

**位置：** `RatingController.java` submit()

**问题：** 
```java
dramaRepository.findById(dramaId).ifPresent(d -> {
    d.setRating(Math.round(avg * 10.0) / 10.0);
    dramaRepository.save(d);
});
```
两个用户同时评分时，后写入的可能覆盖前一个计算的 avg 值（虽然 avg 是从 DB 查的，但 findById→setRating→save 之间存在竞态）。

**修复代码：**

```java
// DramaRepository.java
@Modifying
@Transactional
@Query("UPDATE Drama d SET d.rating = :rating WHERE d.id = :id")
void updateRating(@Param("id") Long id, @Param("rating") Double rating);

// RatingController.java 或提取到 RatingService
dramaRepository.updateRating(dramaId, Math.round(avg * 10.0) / 10.0);
```

---

#### M8. InteractionService.addPoints() — 积分操作非线程安全

**位置：** `InteractionService.java` addPoints()

**问题：** `user.setPoints(user.getPoints() + points)` + `userRepository.save(user)` 是读-改-写模式。两个并发答题请求可能导致积分丢失。

**修复代码：**

```java
// UserRepository.java
@Modifying
@Transactional
@Query("UPDATE User u SET u.points = u.points + :points WHERE u.id = :id")
void addPoints(@Param("id") Long id, @Param("points") int points);

// InteractionService.java
private void addPoints(User user, int points) {
    userRepository.addPoints(user.getId(), points);
}
```

注意：如果后续需要获取更新后的积分值，应在调用后再查一次，或使用 `@Version` 乐观锁。

---

#### M9. SecurityConfig — /api/comment/** permitAll 范围过宽

**位置：** `SecurityConfig.java`

**问题：** `.requestMatchers("/api/comment/**").permitAll()` 使得 POST 评论和 POST 点赞也不需要认证。虽然 `AuthUtils.requireUserId()` 在无 token 时会抛 BusinessException(401)，但这是逻辑层面的保护而非 Spring Security 层面的拦截。应该让需要认证的接口走 Spring Security 的认证链。

**修复代码：**

```java
// SecurityConfig.java
.requestMatchers(HttpMethod.GET, "/api/comment/**").permitAll()       // GET 查询公开
.requestMatchers(HttpMethod.GET, "/api/comment/{interactionId}/count").permitAll()
// POST /api/comment 和 POST /api/comment/{id}/like 走 anyRequest().authenticated()
```

---

#### M10. 无 CORS 配置

**位置：** `SecurityConfig.java`

**问题：** 前端和后端可能不同源（如前端 localhost:3000 → 后端 localhost:8080），没有 CORS 配置会导致跨域请求被浏览器拦截。

**修复代码：**

```java
// SecurityConfig.java — 在 filterChain 方法中添加
@Bean
public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
    http.cors(cors -> cors.configurationSource(corsConfigurationSource()))
        .csrf(csrf -> csrf.disable())
        // ... 其余配置不变
}

@Bean
public CorsConfigurationSource corsConfigurationSource() {
    CorsConfiguration configuration = new CorsConfiguration();
    configuration.setAllowedOrigins(List.of("http://localhost:3000", "http://localhost:8080"));
    configuration.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "OPTIONS"));
    configuration.setAllowedHeaders(List.of("*"));
    configuration.setAllowCredentials(true);
    UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
    source.registerCorsConfiguration("/api/**", configuration);
    return source;
}
```

---

#### M11. API 分页无上限

**位置：** 所有 Controller 的 page/size 参数

**问题：** page 和 size 参数没有最大值限制，可传 `size=999999` 导致一次查询返回全量数据，可能 OOM。

**修复代码：**

```java
// 方案一：在每个 Controller 中限制
@GetMapping("/recommend")
public ApiResponse<Page<DramaSummary>> recommend(
        @RequestParam(defaultValue = "0") @Min(0) int page,
        @RequestParam(defaultValue = "10") @Min(1) @Max(100) int size) {
    return ApiResponse.success(dramaService.getRecommended(page, size));
}

// 方案二（推荐）：统一分页 DTO
@Data
public class PageRequest {
    @Min(0)
    private int page = 0;
    @Min(1) @Max(100)
    private int size = 10;
}
```

---

#### M12. OnlineService Redis 心跳 30秒过期 + 全量重设 TTL

**位置：** `OnlineService.java` heartbeat()

**问题：** 每次 heartbeat 都执行 `redisTemplate.opsForSet().add(key, ...)` + `redisTemplate.expire(key, 30, TimeUnit.SECONDS)`。多用户在线时，每次心跳都重设整个 key 的 TTL，效率低且可能导致数据不一致（如果 TTL 重设失败，整个剧集的在线用户数据丢失）。

**修复代码：**

```java
public void heartbeat(Long userId, Long episodeId) {
    if (redisTemplate != null) {
        String key = KEY_PREFIX + episodeId;
        double score = System.currentTimeMillis();
        // 使用 Sorted Set，score 为时间戳
        redisTemplate.opsForZSet().add(key, String.valueOf(userId), score);
        // 清理过期用户（30秒前的）
        redisTemplate.opsForZSet().removeRangeByScore(key, 0, System.currentTimeMillis() - HEARTBEAT_TTL_MS);
    } else {
        localOnline.computeIfAbsent(episodeId, k -> new ConcurrentHashMap<>()).put(userId, System.currentTimeMillis());
    }
}

public long getOnlineCount(Long episodeId) {
    if (redisTemplate != null) {
        String key = KEY_PREFIX + episodeId;
        // 清理过期用户后返回数量
        redisTemplate.opsForZSet().removeRangeByScore(key, 0, System.currentTimeMillis() - HEARTBEAT_TTL_MS);
        Long count = redisTemplate.opsForZSet().zCard(key);
        return count != null ? count : 0;
    }
    // ... local 逻辑不变
}

public Set<String> getOnlineUsers(Long episodeId) {
    if (redisTemplate != null) {
        String key = KEY_PREFIX + episodeId;
        redisTemplate.opsForZSet().removeRangeByScore(key, 0, System.currentTimeMillis() - HEARTBEAT_TTL_MS);
        return redisTemplate.opsForZSet().range(key, 0, -1);
    }
    // ... local 逻辑不变
}
```

---

### 🟢 轻微（清理 / 文档 / 非功能性）

---

#### L1. coverUrl 仍指向视频文件

**位置：** `DataInitializer.java`

**问题：** `d.setCoverUrl("/videos/北派寻宝笔记/第63集.mp4")` 等全部指向 mp4 文件，而非封面图片。前端无法正确显示封面。

**修复代码：**

```java
// 修改所有 setCoverUrl 调用，使用 cover 目录下的图片
d.setCoverUrl("/covers/beipai-xunbao.jpg");         // 北派寻宝笔记
d.setCoverUrl("/covers/tianxia-wanku.jpg");          // 天下第一纨绔
d.setCoverUrl("/covers/shibasui-grandma.jpg");       // 十八岁太奶奶
d.setCoverUrl("/covers/xingde-xiangyu.jpg");         // 幸得相遇离婚时
d.setCoverUrl("/covers/huangnian.jpg");              // 荒年全村啃树皮
d.setCoverUrl("/covers/jiali-jiawai.jpg");           // 家里家外

// 并在 static/covers/ 目录下放置对应的封面图片
```

---

#### L2. android/ 目录仍是 WebView 空壳

**位置：** `android/` 目录

**问题：** MainActivity.kt 只加载 preview.html，没有原生功能。README 却声称已实现 Kotlin + Jetpack Compose + ExoPlayer。

**修复建议：** 删除 `android/` 目录或在 README 中标注为"实验性/规划中"，避免误导。

---

#### L3. k8s/ 目录内容不可访问

**位置：** `k8s/` 目录

**问题：** K8s 配置文件可能为空壳或链接 404。

**修复建议：** 删除空壳目录或补充有效的 deployment.yaml/service.yaml 配置。

---

#### L4. README 未更新

**位置：** `README.md`

**问题：** 
1. 缺少 auth 相关接口文档（register/login/me）
2. 缺少 comment/rating/online 接口文档
3. Android/K8s 描述过时
4. 数据库设计缺少 ratings、comments、comment_likes、user_eggs 表

**修复建议：** 重写 README，添加完整 API 文档和项目状态说明。

---

#### L5. deploy.sh 未更新

**位置：** `deploy.sh`

**问题：** 缺少 auth 接口验证、无健康检查重试、无回滚逻辑。

**修复建议：** 重写 deploy.sh，添加健康检查循环和回滚机制。

---

#### L6. OnlineService ScheduledExecutorService 未正确关闭

**位置：** `OnlineService.java`

**问题：** `ScheduledExecutorService cleaner` 在应用关闭时未调用 `shutdown()`，可能导致线程泄漏。

**修复代码：**

```java
@PreDestroy
public void cleanup() {
    cleaner.shutdown();
    try {
        if (!cleaner.awaitTermination(5, TimeUnit.SECONDS)) {
            cleaner.shutdownNow();
        }
    } catch (InterruptedException e) {
        cleaner.shutdownNow();
        Thread.currentThread().interrupt();
    }
}
```

---

#### L7. ProgressService.reportProgress() — 缺少 @Transactional

**位置：** `ProgressService.java`

**问题：** `reportProgress()` 方法包含读（findByUserIdAndEpisodeId）和写（save），但没有 `@Transactional` 注解。虽然单个 save 在 Spring Data JPA 中默认有事务，但读+写应在一个事务中保证一致性。

**修复代码：**

```java
@Transactional
public void reportProgress(ProgressReport report, Long userId) {
    // ... 逻辑不变
}
```

---

#### L8. GlobalExceptionHandler 完整性无法确认

**问题：** 源码文件无法通过 GitHub raw 访问确认。需确认是否处理了以下异常类型：
- `MethodArgumentNotValidException`（@Valid 校验失败）
- `ConstraintViolationException`（JPA 约束违反）
- `HttpMessageNotReadableException`（请求体解析失败）
- `AccessDeniedException`（Spring Security 访问拒绝）
- `AuthenticationException`（认证失败）

**修复代码（完整版）：**

```java
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public ApiResponse<Void> handleBusiness(BusinessException e) {
        return ApiResponse.error(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ApiResponse<Void> handleValidation(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
                .map(fe -> fe.getField() + ": " + fe.getDefaultMessage())
                .collect(Collectors.joining("; "));
        return ApiResponse.error(400, message);
    }

    @ExceptionHandler(HttpMessageNotReadableException.class)
    public ApiResponse<Void> handleNotReadable(HttpMessageNotReadableException e) {
        return ApiResponse.error(400, "请求体格式错误");
    }

    @ExceptionHandler(AccessDeniedException.class)
    public ApiResponse<Void> handleAccessDenied(AccessDeniedException e) {
        return ApiResponse.error(403, "无访问权限");
    }

    @ExceptionHandler(Exception.class)
    public ApiResponse<Void> handleException(Exception e) {
        log.error("Unexpected error", e);
        return ApiResponse.error(500, "服务器内部错误");
    }
}
```

---

## 三、问题总览

| 编号 | 问题 | 严重程度 | 类型 |
|------|------|----------|------|
| S1 | syncViewCountToDatabase 读-改-写 | 🔴 严重 | 并发安全 |
| S2 | EpisodeController userId 从 query param 获取 | 🔴 严重 | 安全隐患 |
| S3 | OnlineController userId 从请求体获取 | 🔴 严重 | 安全隐患 |
| S4 | InteractionPoint.options LAZY 无事务 | 🔴 严重 | 功能缺陷 |
| S5 | Redis viewCount 1小时过期丢数据 | 🔴 严重 | 数据丢失 |
| S6 | AuthController.getUser() 信息泄露 | 🔴 严重 | 安全隐患 |
| S7 | JwtUtil 默认密钥不安全 | 🔴 严重 | 安全隐患 |
| S8 | JwtFilter 静默吞异常 | 🔴 严重 | 安全/调试 |
| M1 | getRelatedDramas 小规模 N+1 | 🟡 中等 | 性能 |
| M2 | RatingController 使用 Map 非 DTO | 🟡 中等 | 代码质量 |
| M3 | CommentRequest 缺少参数校验 | 🟡 中等 | 代码质量 |
| M4 | CommentService.toResponse N+1 | 🟡 中等 | 性能 |
| M5 | replyCountMap 计算逻辑错误 | 🟡 中等 | Bug |
| M6 | toggleLike likeCount 读-改-写 | 🟡 中等 | 并发安全 |
| M7 | RatingController drama.rating 读-改-写 | 🟡 中等 | 并发安全 |
| M8 | addPoints 积分读-改-写 | 🟡 中等 | 并发安全 |
| M9 | /api/comment/** permitAll 过宽 | 🟡 中等 | 安全 |
| M10 | 无 CORS 配置 | 🟡 中等 | 功能缺陷 |
| M11 | API 分页无上限 | 🟡 中等 | 安全/性能 |
| M12 | OnlineService Redis 心跳效率低 | 🟡 中等 | 性能 |
| L1 | coverUrl 指向视频文件 | 🟢 轻微 | 数据错误 |
| L2 | android/ 空壳 | 🟢 轻微 | 文档/清理 |
| L3 | k8s/ 空壳 | 🟢 轻微 | 文档/清理 |
| L4 | README 未更新 | 🟢 轻微 | 文档 |
| L5 | deploy.sh 未更新 | 🟢 轻微 | 运维 |
| L6 | ScheduledExecutorService 未关闭 | 🟢 轻微 | 资源泄漏 |
| L7 | ProgressService 缺少 @Transactional | 🟢 轻微 | 代码质量 |
| L8 | GlobalExceptionHandler 完整性待确认 | 🟢 轻微 | 代码质量 |

---

## 四、建议修复顺序

### 第一批（安全+数据安全，1天）
1. **S7** JwtUtil 默认密钥 → 防止 token 伪造
2. **S6** AuthController.getUser() → 防止用户信息泄露
3. **S8** JwtFilter 异常日志 → 可观测性
4. **S2** EpisodeController userId → 统一鉴权
5. **S3** OnlineController userId + SecurityConfig → 统一鉴权
6. **M9** /api/comment/** 权限细化 → 精确访问控制

### 第二批（并发安全+数据完整性，1天）
7. **S1** syncViewCountToDatabase → 原子操作
8. **S5** Redis viewCount 过期策略 → 防止数据丢失
9. **M8** addPoints → 原子操作
10. **M7** drama.rating 更新 → 原子操作
11. **M6** toggleLike likeCount → 原子操作

### 第三批（N+1 + LazyLoading，1天）
12. **S4** InteractionPoint LAZY → JOIN FETCH
13. **M1** getRelatedDramas N+1 → 批量查询
14. **M4** CommentService N+1 → 批量查询
15. **M5** replyCountMap 逻辑修复 → 正确计算

### 第四批（DTO + 校验 + CORS，1天）
16. **M2** RatingRequest DTO
17. **M3** CommentRequest 校验
18. **M10** CORS 配置
19. **M11** 分页上限
20. **M12** OnlineService Redis 改 Sorted Set
21. **L8** GlobalExceptionHandler 完善

### 第五批（清理+文档，0.5天）
22. **L1** coverUrl 修复
23. **L6** ScheduledExecutorService 关闭
24. **L7** ProgressService @Transactional
25. **L2** android/ 目录处理
26. **L3** k8s/ 目录处理
27. **L4** README 重写
28. **L5** deploy.sh 重写

---

## 五、对比改进计划的完成度

基于 `short-drama-player-improvement-plan.md` 的九个阶段：

| 阶段 | 内容 | 完成度 | 遗留问题 |
|------|------|--------|----------|
| 一 | 安全补底 | 85% | 默认密钥未移除、AuthController 信息泄露、CORS 缺失 |
| 二 | 数据模型修正 | 70% | syncViewCount 仍读-改-写、Redis 统计无落盘机制、各处读-改-写 |
| 三 | 异常处理与API规范 | 75% | GlobalExceptionHandler 完整性待确认、DTO 不完整、分页无上限 |
| 四 | 互动体验核心改造 | 80% | LAZY 加载无事务、积分系统非原子、彩蛋图鉴未实现 |
| 五 | 前端工程化 | 50% | preview.html 仍为单文件、API 层对齐未完成 |
| 六 | 搜索优化 | 30% | 搜索未包含剧集标题、无排序优化 |
| 七 | Android 端 | 0% | 空壳未清理 |
| 八 | 部署与运维 | 60% | docker-compose 已改进、deploy.sh 未更新、k8s 空壳 |
| 九 | 代码质量 | 10% | 无单元测试、README 过时、无 LICENSE |

package com.drama.repository;

import com.drama.model.InteractionOption;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InteractionOptionRepository extends JpaRepository<InteractionOption, Long> {
}
